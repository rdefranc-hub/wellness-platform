// Minimal shim for react-resizable-panels.
export const Panel = (props: any) => props?.children ?? null;
export const PanelGroup = (props: any) => props?.children ?? null;
export const PanelResizeHandle = (props: any) => null;
export default {};
