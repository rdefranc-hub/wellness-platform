      # Build do frontend a partir da RAIZ (usa vite.config.ts na raiz)
      - name: Build Vite (root)
        run: |
          npx vite build || npm run build
          echo "PÓS-BUILD: listagens para conferência"
          ls -la
          ls -la client || true
          ls -la client/dist || true
        env:
          CI: "false"
